# 工作日誌 · 2026-07-12（免驚 AI · Claude Code 安裝教學包）

## 一句話總結
昨天暫停中的網站修改（①章手動安裝/訂閱教學/AI截圖除錯、⑨章MCP除錯）今天正式 commit push 上線；報名表單完成最後一輪內容微調；發現並補上手動安裝流程漏掉的「終端機設定」步驟。

---

## 今天完成的事

### 1. 報名表單 Q7／Q10 最後一輪微調
- Q7（大型專案選一實作）：拿掉「貪食蛇」點名（改「小遊戲練習」），新增第 5 個選項「深度寫作系統」（對應⑬章 Taiwan.md 專案，強調「查證資料來源、標好註腳」是賣點），但後來確認 **Playwright MCP 不放進 Q7**——使用者說明 MCP 應該是大家都要裝的基礎項目（跟裝 Claude Code 同一層級），不是投票競爭的選項，所以 Q7 最終定案為 4 選項：知識庫／英語遊戲／AI陪讀／查證式寫作系統。
- Q10（Pro帳號準備）：題目本身直接寫出三個選擇（已經有了／自己課前訂閱／希望現場帶著操作），不是只靠選項才看得出來。
- 新增 `docs/LINE通知範本.md`：範本1（確認名額尚未收款）、範本2（確認收款正式報名成功）、**範本3（訂閱 Claude Pro 圖文步驟，含「Pro跟Max選左邊」提醒）**。

### 2. ⑨ MCP 章新增「萬一安裝卡住了」段落（subagent 執行，commit fa4a902）
- 背景 subagent 查證 `claude mcp add playwright -- npx @playwright/mcp@latest` 這行安裝指令目前仍正確，未變動。
- 新增段落涵蓋三種常見卡關（npx 首次下載較慢／npm 抓不到指令／防火牆權限彈窗），結尾沿用①章「截圖問AI」的同一套建議，三語都有，渲染驗證通過（投影片 7→8 張，零錯誤）。

### 3. 網站正式 commit push 上線（commit fa4a902）
- 使用者指示解除暫停，把①②章所有累積的修改（手動安裝備用步驟、訂閱Claude Pro圖文教學、卡關段落改「先截圖問AI」、⑨章MCP除錯）連同 docs/ 一起推上 GitHub Pages。
- 已確認線上版本（https://meicy321.github.io/claude-code-setup/）部署完成，內容比對正確。

### 4. 發現①章手動安裝六步驟漏掉一步：終端機設定
- 使用者指出：手動安裝流程沒有涵蓋一鍵腳本會做的「把 VS Code 預設終端機設成 Git Bash＋寫入 `CLAUDE_CODE_GIT_BASH_PATH` 到 `~/.claude/settings.json`」這一步——沒有這步，`claude` 指令在終端機裡可能用不起來。
- 對照 `install-claude-code.ps1` 原始碼確認腳本實際做法後，補上新的「第 6 步：設定終端機」（VS Code 內 `Ctrl+Shift+P` → 開啟使用者設定 JSON → 加一行 `terminal.integrated.defaultProfile.windows: Git Bash`），原本的「測試看看」順延成第 7 步。三語（index/en/tai.html）都已修改，**尚未 commit/push、尚未跑渲染驗證**（驗證指令被使用者中斷，要接續做）。
- 討論但未解決：使用者問能不能截圖「真正的 VS Code 畫面」——我方誠實說明沒有桌面級螢幕操作能力（Chrome分頁自動化工具管不到 VS Code 這種桌面應用），提供兩個替代方案給使用者選：(a) 做一個外觀模擬 VS Code 的 HTML 假畫面截圖（今天稍早幫 Karpathy 維基/Taiwan.md 文章展示用過的同一招）(b) 使用者自己截圖傳來，我幫忙裁切加註解。**使用者尚未回覆選哪個。**

---

## 成品連結
- 三語講義（書，14 章）：https://meicy321.github.io/claude-code-setup/
- GitHub Repo：https://github.com/meicy321/claude-code-setup
- 報名表單定稿：`docs/報名表單-nanoclaw執行prompt.md`（尚待 nanoclaw 實際建出 Google 表單）
- LINE 通知範本：`docs/LINE通知範本.md`

---

### 5. ①章第6步改成逐步清單，並拆分終端機設定與測試（commit 2cc9b24, 上線）
- 使用者一開始要求把「終端機 Git Bash 設定」併回原本步驟（不要獨立一張卡），後來又指出寫得太抽象看不懂（「Ctrl+Shift+P」「大括號」這種說法對新手是新東西）。改寫成 7 個具體小動作的有序清單（`<ol>`），每步講清楚按什麼鍵、點哪裡，三語同步，渲染驗證通過（12 step，零錯誤），已 commit push 上線並確認部署完成。

### 6. 找到並引用兩篇外部文章當補充資源（commit 9820ea8, 上線）
- 使用者分享 second-brain workshop（gatelynch.github.io）文章，抓重點但判斷「不放進網站」——內容適合當使用者自己準備 Q7 知識庫模板的參考（四層資料夾架構 raw/wiki/brainstorming/artifacts、Markdown 最低識字量三符號等），不是給完全新手看的教材。
- 使用者分享 Matt Pocock 團隊的 Skill 撰寫心得文章（botsnova labs，講 SKILL.md 三層漸進揭露＋description 撰寫技巧），使用者選擇「B：⑧章加一個進階選讀小節」。新增「更進階：想自己動手寫 SKILL.md？三個訣竅」小節（第三人稱/寫何時用/寫觸發條件不寫流程），三語同步，來源引用已加進本章參考。
- ④章 AI原理參考資源新增 Transformer Explainer（poloclub.github.io，喬治亞理工做的瀏覽器內即時 GPT-2 視覺化，查證為免費、standalone、適合連結）。
- 用 Explore/WebFetch 查證 Matt Pocock skills repo 其餘技能清單，額外挑出 `/grill-with-docs`（跟 grill-me 同源但訪談完自動寫成文件）加進⑧章 grill-me 段落當「想再多走一步」補充。三語同步，commit 6c5588e 已上線。

### 7. ⑭章新增 Git 學習資源＋Happy Coder 手機遙控（commit 9820ea8, 上線）
- 使用者要找 Git 教學影片，先列出候選（中文YouTube／freeCodeCamp／Learn Git Branching），使用者選定「中文YouTube影片＋Learn Git Branching互動網站」（英文介面搭配 Papago 對照）。新增「Git」小節於⑭章，三語同步。
- 使用者問「Happy Coder 手機看這個專案」牽出一個重要安全問題：使用者原本用**公司共用 Claude 帳號**準備教材，經討論釐清風險核心是「帳號是誰的」而非「在哪個資料夾工作」——若為多人共用登入的同一組帳號密碼，網頁版側邊欄任何登入者都看得到全部對話紀錄；若為 Team/Enterprise 方案每人獨立帳號則無此風險。使用者後來確認**已改用自己的個人帳號**（`/login` 登入自己帳號），疑慮解除。
- 查證 Happy Coder 技術文件（happy.engineering）：QR code 配對＋端對端加密（同 Signal 協定，ECDH+AES-256-GCM），中繼伺服器只存加密內容，App本身安全無虞，風險只在帳號共用與否。新增「手機」小節於⑭章介紹 Happy Coder，附共用帳號風險提醒。三語同步。
- 討論但決定不做：使用者問可否用 Shadow DOM——技術上可行但確認使用者網站目前是獨立 GitHub Pages、不需要嵌入別的網站，暫無實際需求，未動作。

### 8. ①②章複製按鈕支援多處複製＋每項安裝補上「為什麼」（本次對話結尾，尚未commit）
- 使用者從手機截圖指出：①章「接下來只要做這3件事」投影片裡提到「把老師給你的那一行貼上」，但指令本身只出現在最前面那張投影片，翻到這頁想複製要跳回去找。要求在這頁也放一份可複製的指令。
- 改寫複製按鈕 JS：從綁定單一 `id="copyBtn"` 改成 `querySelectorAll('.copy-btn[data-copy-cmd]')` 逐一綁定，讓同一頁可以有多個複製按鈕共用同一份指令文字。在步驟2卡片內新增一個縮小版 `.cmd-card`（沿用既有樣式，字級縮小）。
- 使用者從另一張截圖指出「背後：這一行指令幫你打理好這些」清單裡，Git／Node.js／Claude Code／VS Code 每一項只寫「做了什麼」沒寫「為什麼」（特別點名問 Node.js）。改寫每一行加上明確原因：Git（記錄修改的時光機，Claude Code離不開它）、Node.js（⑨章MCP要靠它跑，先裝好避免卡關）、Claude Code（官方原生非npm，才能自動更新不被Node版本綁死）、VS Code（之後每天工作的主畫面）。
- 兩項修改（多處複製按鈕＋為什麼原因）同步套用到②章 Statusline（index/en/tai.html 與 statusline/en/tai.html 共6檔），中文版已用 puppeteer 渲染驗證通過（2 copy 按鈕、零錯誤、第二顆按鈕點擊確實觸發複製提示）；**六檔案全部改完但尚未做完整三語渲染驗證、尚未 commit**（驗證指令被使用者中斷，要求先存日誌）。

---

## ⏳ 下次接續時的待辦清單
1. **最優先**：完成 index/en/tai.html + statusline/en/tai.html 共6檔的渲染驗證（多處複製按鈕是否都能點擊複製、pipe-row為什麼說明有沒有溢出排版），驗證過再問使用者是否一起 commit push。
2. **VS Code 截圖方案待使用者選**：假畫面 HTML 模擬 vs 使用者自行截圖——這個問題從7/11提出至今尚未有結論，①章第6步（終端機設定）目前仍是純文字/有序清單，沒有配圖。
3. 使用者仍在自行校對台語（⑬⑭章＋⑪帶做區＋今天新增的多處內容），完成前這三項維持暫停：拿掉全站 CC BY-NC-SA 授權聲明／打包 zip／GitHub repo 設為私有。
4. nanoclaw 尚未實際執行 `docs/報名表單-nanoclaw執行prompt.md` 建出真正的 Google 表單——等建好要回報編輯連結／發布連結。
5. 表單建好後：準備 Drive 資料夾＋依 Gmail 逐一加共用權限的實際操作流程。
6. （可選）大型專案 4 選項（知識庫/英語遊戲/AI陪讀/查證式寫作系統），每個都要準備一份可現場客製化的起始模板——目前只有討論，尚未動手做任何一份。MCP已確認不放進投票選項，是全班都要裝的基礎項目。
